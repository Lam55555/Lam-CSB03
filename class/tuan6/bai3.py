def lonhon():
    a = float(input('Print a number: '))
    b = float(input('Print a number: '))
    if(a>b):
        return print(f"{a} lớn hơn {b}")
    else:
        return print(f"{a} bé hơn {b}")
lonhon()